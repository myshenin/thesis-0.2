vesion: 2
jobs:
  server:
    docker:
      - image: myshenin/thesis:0.3
    steps:
      - checkout
      - run: ls -a
      - run: cd thesis/server/test && npm install
      - run: npm run test

workflows:
  version: 2
  integration:
    jobs:
      - server
deployment:
  staging:
    branch: master
    codedeploy:
      myshenin.thesis:
        deployment_group: my-deployment-group
