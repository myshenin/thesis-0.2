{
  "name": "server-test",
  "version": "0.0.0",
  "description": "Tests for AWS Lambdas of Thesis project",
  "main": "index.js",
  "scripts": {
    "test": "cucumberjs ./features -r ./features",
    "test:tsne": "cucumberjs ./features -r ./features --tags @tsne",
    "test:weather": "cucumberjs ./features -r ./features --tags @weather",
    "test:result": "cucumberjs ./features -r ./features --tags @result"
  },
  "author": "Anton Myshenin",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.192.0",
    "cucumber": "^3.2.1",
    "request": "^2.83.0",
    "request-promise": "^4.2.2"
  }
}
