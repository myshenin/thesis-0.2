import React from 'react';

const StartButton = (props) => {
    return (
        <div className="start-button">
            <button>ПУСК</button>
        </div>
    );
};

export default StartButton;